# Omni-Retrieval Base Configuration
# Phase 2: Hybrid Search + Reranking + Baseline Eval

llm:
  model_name: "qwen-flash"
  api_base: "https://dashscope-intl.aliyuncs.com/compatible-mode/v1"
  temperature: 0.1
  max_tokens: 1024

embedding:
  model_name: "text-embedding-v4"
  api_base: "https://dashscope-intl.aliyuncs.com/compatible-mode/v1"
  dimensions: 1024

chunking:
  chunk_size: 512
  chunk_overlap: 50

qdrant:
  host: "localhost"
  port: 6333
  collection_name: "hotpotqa"
  collection_name_hybrid: "hotpotqa_hybrid"

reranking:
  model_name: "qwen3-rerank"
  api_base: "https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank"
  top_n: 5