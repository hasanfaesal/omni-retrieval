# Ruff configuration
target-version = "py310"
line-length = 88

[lint]
select = ["E", "F", "I", "W", "UP"]
ignore = ["E501"]  # Line length handled by formatter

[lint.isort]
known-first-party = ["src"]
